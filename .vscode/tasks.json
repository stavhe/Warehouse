{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "make",
           // "args": ["-g", "source.cpp", "-o", "output"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}